{
  "name": "golden-hour-mas",
  "version": "1.0.0",
  "description": "Golden Hour — Multi-Agent Autonomous Clinical Escalation System (MERN)",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install"
  }
}
